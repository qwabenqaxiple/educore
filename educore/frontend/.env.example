# Frontend environment variables — copy to .env
# For local dev the Vite proxy handles /api, so this can stay empty
# For production (Vercel/Netlify) set to your backend URL
VITE_API_URL=http://localhost:5000/api
